import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["localhost", "127.0.0.1", "192.168.1.4", "gratta-eight.vercel.app", "gratta-k2m2.vercel.app"], 
};

export default nextConfig;
